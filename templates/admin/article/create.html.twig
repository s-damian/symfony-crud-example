{% extends 'layouts/admin.html.twig' %}

{% block content %}
    <h1>
        {% trans %}article.title.create{% endtrans %}
    </h1>

    {{ form(form) }}

    <div>
        <a href="{{ path('admin_article_index') }}">
            {% trans %}article.back_to_index{% endtrans %}
        </a>
    </div>
{% endblock %}